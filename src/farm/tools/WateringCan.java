package farm.tools;

import farm.Tools;

public class WateringCan extends Tools {

    /**
     * Class constructor for creating a WateringCan object
     * from the Tools constructor
     * It sets all attributes to default values
     *
     */
    public WateringCan() {
        super("Watering Can", "Adds to the total number of times a crop has been watered. ", 0, 0.5);
    }
}

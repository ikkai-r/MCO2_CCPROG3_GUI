package farm.tools;

import farm.Tools;

public class Shovel extends Tools {

    /**
     * Class constructor for creating a Shovel object
     * from the Tools constructor
     * It sets all attributes to default values
     *
     */
    public Shovel() {
        super("Shovel", "Removes a withered plant from a tile.", 7, 2);
    }
}

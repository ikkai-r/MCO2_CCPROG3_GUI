package farm.tools;

import farm.Tools;

public class Shovel extends Tools {
    public Shovel() {
        super("Shovel", "Removes a withered plant from a tile. Can be used on any tile/crop with varying effect, " +
                "as described above.", 7, 2);
    }
}
